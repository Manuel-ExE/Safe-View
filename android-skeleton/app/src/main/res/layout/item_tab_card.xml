<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/tab_card_bg">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="8dp"
        android:paddingEnd="4dp"
        android:paddingTop="4dp">

        <TextView
            android:id="@+id/tabCardTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="New tab"
            android:textColor="@color/sv_text"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end" />

        <ImageButton
            android:id="@+id/tabCardClose"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:src="@drawable/ic_close"
            android:scaleType="centerInside"
            android:padding="6dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Close tab" />
    </LinearLayout>

    <ImageView
        android:id="@+id/tabCardThumb"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_margin="6dp"
        android:scaleType="centerCrop"
        android:background="@color/sv_input"
        android:contentDescription="@null" />
</LinearLayout>
